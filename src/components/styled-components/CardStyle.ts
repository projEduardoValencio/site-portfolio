import styled from 'styled-components';

export const Card = styled.div`
  display: flex;
  width: 100%;
  background: var(--accent-color);
  border-radius: 15px;
  filter: drop-shadow(5px 5px 10px #0000001f);
`;
